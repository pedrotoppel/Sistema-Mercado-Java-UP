package model.interfaces;

/**
 * INTERFACE (contrato).
 * Toda classe que implementar Descontavel e obrigada a dizer
 * como calcula o proprio desconto.
 */
public interface Descontavel {
    double calcularDesconto();
}
